export interface ShopSummary {
  id: string;
  name: string;
  code: string;
  isActive: boolean;
  planName: string;
  planCode: string;
  branchCount: number;
  userCount: number;
  createdAt: string;
}
